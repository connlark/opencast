import Foundation

enum OPMLImportCopy {
    static let subscriptionsOnlyFooter: LocalizedStringResource = "OPML includes subscriptions only. Listening progress, downloads, queues, refresh logs, and local cache stay on this device."
}
